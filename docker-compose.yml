version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: polymarket-bot-db
    environment:
      POSTGRES_DB: polymarket_bot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Trading Bot (TODO: Uncomment when ready for production)
  # bot:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: polymarket-bot
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     - DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/polymarket_bot
  #     - ENVIRONMENT=production
  #     - PAPER_TRADING=false
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./logs:/app/logs
  #   restart: unless-stopped

  # Dashboard API (TODO: Uncomment when ready)
  # dashboard:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.dashboard
  #   container_name: polymarket-bot-dashboard
  #   depends_on:
  #     - postgres
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/polymarket_bot
  #   env_file:
  #     - .env
  #   restart: unless-stopped

volumes:
  postgres_data:
